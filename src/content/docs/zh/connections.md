# 连接配置

GoNavi 将常见连接能力收敛到同一工作流里，减少不同数据库之间的心智切换。

## 支持的输入方式

- 主机 / 端口 / 数据库名
- URI
- 用户名 / 密码
- SSH 隧道
- 代理设置

## 实践建议

1. 把高频数据库放在同一命名约定下，便于团队共享。
2. 对需要跳板机的环境，优先走 SSH 隧道而不是临时手工转发。
3. 对预发和生产环境保持明确区分，避免误连。
